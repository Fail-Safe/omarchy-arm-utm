# Downloaded base images (~900 MB) — the script downloads and verifies them
dl/

# Build disks (tens of GB)
vm/
build-test/

# The distributable image: 6.5 GB, does not fit in a repository.
# Published as a release. The README and sha256 are versioned.
dist/*.zip
dist/*.utm/

# Execution outputs
logs/
provision/*.iso
provision/repair-iso/*.log

# Only the two captures used in the documentation are versioned; the rest are
# debug snapshots (39 MB) that provide no value to those who clone the repo.
shots/*
!shots/hires.png
!shots/dist-final.png

# macOS
.DS_Store

# Drafts of messages for Omarchy discussions
publicar/

# Versioned and reconciliation build directories
build-*/
